# Resizing-an-image
This project demonstrates how to resize an image using Python and the OpenCV and imutils libraries. The main objective is to modify the image dimensions while preserving the aspect ratio.  
